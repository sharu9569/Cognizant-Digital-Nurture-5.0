import { Component } from '@angular/core'; import { AsyncPipe, NgIf } from '@angular/common'; import { NotificationService } from '../../services/notification.service'; @Component({ selector: 'app-notification', standalone: true, imports: [AsyncPipe, NgIf], providers: [NotificationService], template: '<p *ngIf="notification.message$ | async as message">{{ message }}</p>' }) export class NotificationComponent { constructor(readonly notification: NotificationService) {} } // Component provider creates an isolated scoped instance.
